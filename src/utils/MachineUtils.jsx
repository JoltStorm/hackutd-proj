const machineObj = (name, amount, inputResources, outputResources, costResources) => {
    return { name: name, amount: amount, inputResources: inputResources, outputResources: outputResources, costResources: costResources }
}

export default machineObj